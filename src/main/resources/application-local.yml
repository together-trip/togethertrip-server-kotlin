spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/together_trip
    username: together_trip
    password: together_trip
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true